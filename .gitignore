# Secrets
.env
.env.*
!.env.example

# Python
__pycache__/
*.py[cod]
.venv/
venv/
.pytest_cache/
.ruff_cache/
*.egg-info/

# Node / Next
node_modules/
.next/
out/
npm-debug.log*
package-lock.json

# OS / editor
.DS_Store
*.swp
.idea/
.vscode/

# Build / data
postgres-data/
*.log

# Owner-uploaded brand assets (logo/photo) — runtime, on a Docker volume
backend/media/

# Service-account / OAuth JSON keys (Google Calendar, etc.)
secrets/*
!secrets/.gitkeep
# OAuth secrets, even if written outside secrets/ (the setup script's default out)
gcal-oauth.json
client_secret*.json
